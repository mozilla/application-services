language: rust
os: osx
sudo: required

matrix:
  include:
    - env: TARGET=x86_64-unknown-linux-gnu
      rust: stable
    - env: TARGET=x86_64-unknown-linux-gnu
      rust: nightly

script:
  - scripts/ci-test.sh

cache:
  cargo: true
  directories:
  - libs/ios/
