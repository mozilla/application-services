version: 1.0
about:
  description: A small test of importing and including
  ios:
    class: AppNimbus
    module: App
  android:
    class: .nimbus.AppNimbus
    package: com.example.app
channels:
  - app-debug
  - app-release
import:
  - path: ./lib.fml.yaml
    # The channel here is the one we find in the lib.fml.yaml
    channel: debug
