#include "../../xcconfig/common.xcconfig"

INFOPLIST_FILE = config/Info.plist
HEADER_SEARCH_PATHS = "./RustLog" "./Viaduct"
LIBRARY_SEARCH_PATHS[sdk=iphoneos*][arch=arm64] = "../../target/aarch64-apple-ios/$(buildvariant)";
LIBRARY_SEARCH_PATHS[sdk=iphonesimulator*][arch=arm64] = "../../target/aarch64-apple-ios-sim/$(buildvariant)";
LIBRARY_SEARCH_PATHS[sdk=iphonesimulator*][arch=x86_64] = "../../target/x86_64-apple-ios/$(buildvariant)";
CLANG_ENABLE_MODULES = YES
ALWAYS_SEARCH_USER_PATHS = NO
